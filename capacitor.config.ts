import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'veat_delivery_app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
